name: Test
on:
  workflow_call:
  workflow_dispatch:
  push:
env:
  REP_NAME: "echo $GITHUB_REPOSITORY | cut -d '/' -f 2"
jobs:
  echo_rep_n:
    name: Test triggers
    runs-on: ubuntu-22.04
    steps:
      - name: Test triggers
        run: echo $REP_NAME
